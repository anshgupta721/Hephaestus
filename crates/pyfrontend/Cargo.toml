[package]
name = "pyfrontend"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
simulation = { path = "../simulation" }
onboard_software = { path = "../onboard_software" }
pyo3 = { version = "0.29.2", features = ["extension-module"] }
numpy = "0.29.0"
